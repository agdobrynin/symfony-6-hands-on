<div class="grid grid-cols-1 md:grid-cols-1 lg:grid-cols-2 gap-4">
        {% if posts|length > 0 %}
                {% for post in posts %}
                        {{ include('@mp/include/_post.html.twig', {post, showAvatarInPostBlock: (showAvatarInPostBlock is defined)}) }}
                {% endfor %}
        {% else %}
                There's nothing here yet!
        {% endif %}
</div>
